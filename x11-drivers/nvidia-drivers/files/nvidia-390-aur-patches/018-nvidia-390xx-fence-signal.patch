--- NVIDIA-Linux-x86_64-390.157/kernel/nvidia-drm/nvidia-dma-fence-helper.h.orig	2022-10-12 12:30:31.000000000 +0300
+++ NVIDIA-Linux-x86_64-390.157/kernel/nvidia-drm/nvidia-dma-fence-helper.h	2026-05-07 18:42:02.080325389 +0300
@@ -89,11 +89,12 @@
 #endif
 }
 
-static inline int nv_dma_fence_signal(nv_dma_fence_t *fence) {
+// Rel. commit "dma-buf/dma-fence: Remove return code of signaling-functions" (Philipp Stanner, 1 Dec 2025)
+static inline void nv_dma_fence_signal(nv_dma_fence_t *fence) {
 #if defined(NV_LINUX_FENCE_H_PRESENT)
-    return fence_signal(fence);
+    fence_signal(fence);
 #else
-    return dma_fence_signal(fence);
+    dma_fence_signal(fence);
 #endif
 }
 
